profile: default

allowed_paths:
  - "src/**"
  - "tests/**"
  - ".agent/plans/**"
  - "features.md"

protected_paths:
  - ".github/workflows/**"
  - ".agent-harness/**"

network_profile: package-registries-only
tool_profile: implement-default
timeout_seconds: 7200
max_cost_usd: 50
